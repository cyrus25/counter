export declare type ChangeReadyCallback = (path: string, change: any) => void;
export default class SyncStateRemote {
    changeReadyMap: Map<string, ChangeReadyCallback[]>;
    processChange(clientId: string, path: string, change: any): void;
    onChangeReady(clientId: string, cb: ChangeReadyCallback): () => void;
}
